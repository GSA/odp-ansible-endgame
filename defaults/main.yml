---
# defaults file for odp-ansible-fireeye

endgame_installer_file:
  win: "endgame-agent.exe"
  rpm: "endgame-agent"
  deb: "endgame-agent"
endgame_config_file: endgame-agent.cfg
endgame_api_key:

#Set this to true when this role is used to build AMI  
ami_build_flag: False
secret_text:
access_key:
secret_key:
session_token:
